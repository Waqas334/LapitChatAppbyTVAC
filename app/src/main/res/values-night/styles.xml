<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="materialButtonStyle">@style/My.Button</item>
        <item name="android:buttonStyle">@style/My.Button</item>
        <item name="android:windowBackground">@color/windownBackground</item>


        <!--        This one is for menu text color-->
        <item name="android:actionMenuTextColor">@color/menuTextColor</item>

        <!--        This one is for menu background color-->
        <item name="android:itemBackground">@color/menuBackgroundColor</item>

    </style>


    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/dialogBackgroundColor</item>
        <item name="android:colorAccent">@color/dialogTextColor</item>
    </style>

    <style name="My.Button" parent="@style/Widget.AppCompat.Button">
        <item name="backgroundTint">@color/my_color_selector</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
